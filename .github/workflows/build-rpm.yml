name: Build RPM

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-rpm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install RPM tools
        run: sudo apt-get update && sudo apt-get install -y rpm

      - name: Get version
        id: version
        run: echo "version=$(grep -oP '__version__ = \"\K[^\"]+' src/tts_tester/__init__.py)" >> $GITHUB_OUTPUT

      - name: Setup RPM build
        run: |
          mkdir -p ~/rpmbuild/{BUILD,RPMS,SOURCES,SPECS,SRPMS}
          VERSION=${{ steps.version.outputs.version }}
          DIR=tts-tester-$VERSION
          mkdir -p $DIR
          cp -r src/ data/ LICENSE README.md pyproject.toml $DIR/ 2>/dev/null || true
          tar czf ~/rpmbuild/SOURCES/$DIR.tar.gz $DIR

      - name: Create spec and build
        run: |
          VERSION=${{ steps.version.outputs.version }}
          cat > ~/rpmbuild/SPECS/tts-tester.spec << 'SPEC'
          Name:           tts-tester
          Version:        VERSION_PLACEHOLDER
          Release:        1
          Summary:        
          License:        GPL-3.0-or-later
          URL:            https://github.com/yeager/tts-tester
          Source0:        %{name}-%{version}.tar.gz
          BuildArch:      noarch
          Requires:       python3-gobject gtk4 libadwaita

          %description
          GTK4/Libadwaita application.

          %prep
          %setup -q

          %install
          mkdir -p %{buildroot}/usr/share/tts-tester/tts_tester
          cp src/tts_tester/*.py %{buildroot}/usr/share/tts-tester/tts_tester/
          if [ -d src/tts_tester/locale ]; then
            cp -r src/tts_tester/locale %{buildroot}/usr/share/tts-tester/tts_tester/
          fi

          mkdir -p %{buildroot}/usr/bin
          cat > %{buildroot}/usr/bin/tts-tester << 'LAUNCHER'
          #!/bin/bash
          exec python3 -c "import sys; sys.path.insert(0, '/usr/share/tts-tester'); from tts_tester.main import main; main()" "$@"
          LAUNCHER
          chmod 755 %{buildroot}/usr/bin/tts-tester

          mkdir -p %{buildroot}/usr/share/applications
          cp data/*.desktop %{buildroot}/usr/share/applications/ 2>/dev/null || true

          mkdir -p %{buildroot}/usr/share/icons/hicolor/scalable/apps
          cp data/icons/*.svg %{buildroot}/usr/share/icons/hicolor/scalable/apps/ 2>/dev/null || true

          %files
          %license LICENSE
          %doc README.md
          /usr/bin/tts-tester
          /usr/share/tts-tester/
          /usr/share/applications/*.desktop
          /usr/share/icons/hicolor/scalable/apps/*.svg
          SPEC
          sed -i "s/VERSION_PLACEHOLDER/$VERSION/g" ~/rpmbuild/SPECS/tts-tester.spec
          sed -i "s|\${repo}|tts-tester|g; s|\${PYNAME}|tts_tester|g" ~/rpmbuild/SPECS/tts-tester.spec
          rpmbuild -ba ~/rpmbuild/SPECS/tts-tester.spec

      - name: Upload RPM to release
        if: github.event_name == 'release'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION=${{ steps.version.outputs.version }}
          gh release upload "v$VERSION" \
            ~/rpmbuild/RPMS/noarch/tts-tester-${VERSION}-1.noarch.rpm \
            --clobber
